# Django & Wagtail (Phase 2 Stage 2: Django 2.2 LTS + Wagtail 2.8)
Django>=2.2,<2.3
wagtail>=2.8,<2.9
django-modelcluster>=5.0,<6.0
django-treebeard>=4.3,<5.0

# Web scraping & HTTP
# beautifulsoup4 version controlled by wagtail dependency
requests==2.31.0

# Database
psycopg2-binary==2.9.9

# Configuration
python-decouple==3.8
PyYAML==6.0.1

# Django utilities
django-model-utils>=3.2,<4.0
django-polymorphic>=2.0,<3.0
django-compressor>=4.1,<5.0
# bootstrap-admin removed - not compatible with Django 2.0+ (uses django.core.urlresolvers)

# REST API (DRF 3.9+ compatible with Django 2.2)
djangorestframework>=3.9,<3.10
django-filter>=2.0,<3.0
markdown==3.3.7

# Production server
gunicorn==21.2.0

# Frontend (will be replaced in Phase 2.5)
django-bower==5.0.4

# Testing (compatible with Python 3.7)
pytest==7.4.3
pytest-django==4.5.2
