# Django & Wagtail (Phase 2 Stage 3: Django 3.2 LTS + Wagtail 2.15)
Django>=6.0,<6.1
wagtail>=7.2,<7.3
django-modelcluster>=6.0,<7.0
django-treebeard>=4.5,<5.0

# Web scraping & HTTP
beautifulsoup4==4.12.3
lxml==5.2.2
PyMuPDF==1.24.2  # For parsing PDF files. May require manual pip install due to docker build issue.
requests==2.31.0
openpyxl==3.1.2  # For parsing XLSX files (ministerial meetings)

# Database
psycopg2-binary==2.9.9

# Configuration
python-decouple==3.8
PyYAML==6.0.1

# Django utilities
django-model-utils>=4.1,<5.0
django-polymorphic>=4.2.0
django-compressor>=4.3,<5.0
# bootstrap-admin removed - not compatible with Django 2.0+ (uses django.core.urlresolvers)

# REST API (DRF 3.12+ compatible with Django 3.2)
djangorestframework>=3.15,<3.16
django-filter>=23.3
markdown>=3.3,<4.0
drf-spectacular>=0.27.0  # OpenAPI schema generation and Swagger UI

# Production server
gunicorn==21.2.0

# Frontend (Phase 3.3: Islands Architecture with Vite + React)
django-vite>=3.0.0

# Testing & Quality Assurance (Phase 3.4 & 3.5)
pytest==7.4.3
pytest-django==4.5.2
pytest-cov==4.1.0  # Coverage reporting
factory-boy==3.3.0  # Test fixtures
pytest-xdist==3.5.0  # Parallel test execution

# Code Quality (Phase 3.5)
black==24.1.1  # Code formatter
ruff==0.1.14  # Fast linter (replaces flake8, isort, etc.)
mypy==1.8.0  # Static type checker
django-stubs==4.2.7  # Django type stubs for mypy
types-requests==2.31.0.20240125  # Type stubs for requests
pre-commit==3.6.0  # Git hooks framework
